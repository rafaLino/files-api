pre-commit:
  parallel: true
  jobs:
    - name: types check
      run: pnpm run types:check

    - name: lint check
      run: pnpm run lint:check
      stage_fixed: true

    - name: test
      run: pnpm run test
